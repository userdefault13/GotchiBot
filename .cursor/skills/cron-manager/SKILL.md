---
name: cron-manager
description: >-
  Fleet cron desk via ai-cron-site (cron402). Use for scheduling wakes, listing
  jobs, topup/pause/resume/delete. Paid tools spend USDC — confirm first.
---

# cron-manager (Cursor)

**Read** [`.opencode/skills/cron-manager/SKILL.md`](../../../.opencode/skills/cron-manager/SKILL.md)

Prefer MCP `cron402` (`cron402_guide` first). Credentials via abra project `ai-cron-site`.
