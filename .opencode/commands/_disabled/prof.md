---
description: Prof. Link-Cube — marketplace templates table of contents
---

Open the Prof. Link-Cube template TOC (real packs/playbooks). Prefer the TUI modal via `/prof` (plugin). If the modal is unavailable, run immediately:

```bash
./scripts/prof-toc.mjs --text
```

Or:

```bash
./scripts/gotchibot templates list
```

Do not invent packs. Show stdout to Julius. Stay in this TUI.
