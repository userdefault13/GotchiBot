# 2026-09-15

## Moltbook watch desk (built by starter-link-h1-1, session s20260915-121220-68151)

- `scripts/moltbook-watch.mjs` — read-only Moltbook radar, one cycle per invocation.
  - `GET /agents/me` first; 401 → exit 0 with burned-key message (never crashes cron); no key → graceful skip.
  - Feed (2 pages) + comments on our recent posts + 6 semantic searches (5 api-key queries + our name).
  - Classifies reply-to-us vs api-key-issue; dedup via `sessions/moltbook-watch.json`; findings →
    `sessions/moltbook-watch.log` + `sessions/moltbook-watch/queue.json` (watch + queue ONLY, no write ops).
  - JSON stdout: `{type:'moltbook-watch', status, newReplies, newIssues, queued, ...}`.
- `scripts/moltbook-schedule.mjs` — launchd every 900s (trader-schedule pattern, `lib/launchd-job.mjs`).
  - `status [--json] | install [--dry-run] | uninstall | run-now | plist`.
  - Key NEVER embedded in plist — watch resolves `MOLTBOOK_API_KEY` env or `--env-file`
    (default `~/.config/moltbook/credentials.json`).
- CLI: `./scripts/gotchibot moltbook watch|schedule …` wired.
- Tests passed: syntax, no-key skip, dummy-key 401 path (real API), schedule status/dry-run. NOT installed.
- **MOLTBOOK_API_KEY is believed burned** → rotate in abra (project Aarcade-AIBot) + Moltbook owner
  dashboard (https://www.moltbook.com/login), then write `~/.config/moltbook/credentials.json` and
  `./scripts/gotchibot moltbook schedule install` on the iMac.
- Moltbook API notes: base MUST be `https://www.moltbook.com/api/v1` (www — bare domain strips
  Authorization header). Search: `GET /search?q=…&type=all` → `results[]` with `type` post|comment,
  `post_id`, `author.name`. Comments: tree `comments[].replies`. Rate limit 60 reads/min.