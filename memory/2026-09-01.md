# 2026-09-01

GoBall cartridge deploy fix (home iMac, via Tailscale SSH + remote-ssh.mjs):

- Production minting failed because the home iMac cartridge SIM Docker container
  (aarcade-cartridge-sim, 127.0.0.1:8791, Cloudflare tunnel → cartridge.aarcadeghst.com)
  ran an old build without the goball registration (commit 00dbb948b).
- iMac AarcadeGh-t repo could not `git pull`: HTTPS remote + dead gh token
  (`gh auth status` invalid for userdefault13) + iMac id_ed25519 not on GitHub.
  Workaround: fetched on MBP clone (valid gh auth), ferried commits as a git bundle
  piped over SSH stdin (`cat bundle | remote-ssh.mjs -- 'cat > /tmp/…'`), then
  `git fetch <bundle> refs/remotes/origin/main && git merge --ff-only FETCH_HEAD`.
  iMac now at af5787e14. Bundle refspec must be explicit — bundles advertise no HEAD.
- 4 untracked iMac files collided with newly tracked upstream paths; backed up to
  ~/Dev/aarcade-backups/pre-goball-20260901/ (gotchiOwnership.cjs, Dockerfile,
  Dockerfile.dockerignore, docker-compose.yml). ` M src/style.css` + `?? api/run.sh`
  left in place (upstream doesn't touch them).
- Rebuilt: `docker compose -f services/cartridge-sim-proxy/docker-compose.yml up -d --build`
  → container healthy. Verified: https://aarcadeghst.com/api/cartridge-sim/rules/goball
  returns goball rules JSON (matchModes single/agent_coop, heroSource cAavegotchi).
- iMac gh token revived remotely: piped MBP's valid userdefault13 token over SSH stdin
  into `gh auth login --with-token` + `gh auth setup-git` (token never in argv/logs/disk).
  iMac now fetches GitHub directly (`git ls-remote`/`git fetch` OK). Same OAuth token now
  shared by MBP + iMac — revoking one kills both; a dedicated iMac PAT can be minted on
  github.com later if he wants them separable.
