# 2026-09-06 — Desk audit day

## Harness bug (log from Julius's audit)
`lib/claude-terminal.mjs` sends prompt text + Enter together; the Enter often
doesn't register, so prompts sit unsubmitted and the next prompt's tail gets
appended. **Fix: send Enter as a separate keystroke ~1–2s after the text.**
link-verify was `--restarted` during the audit and lost cross-cycle context.

## Audit corrections (from Julius, verified)
- "2,647 skipped fills" is a **lifetime** counter: 2,018 were `Base RPC is not
  configured` (Jul 11–Aug 27, already fixed). Only live skip reason: `No liquid
  direct Uniswap USDC pool` (434).
- "+$66.8k open mark" is **not** supported — 14d total: 112 fills, +$2,198.
- Hub "iMac 0 running" doesn't count the three standing verifiers (they're up).
- Trader API: MBP zombie on :4000 (orphaned `npm run dev`, empty node_modules)
  was killed; **iMac :4000 is the real, healthy API**.
- YFI / infra-monitor: healthy (sessions/infra-logs/, 5-min cadence on iMac).

## Actions executed (2026-09-06)
1. **MBP crontab fixed** — check-claude-terminal cron now has PATH +
   absolute node (`/Users/juliuswong/.nvm/versions/node/v22.9.0/bin/node`).
   Was failing 572× (`node: command not found`). Installed from staged
   `mbp-crontab.new` (backup `mbp-crontab.bak`).
2. **Gotchi-Trader backfill applied** — `backfill-positions-2026-09-06.sql`:
   INSERT 0 105 (idempotent, NOT EXISTS guard). Ledger now 122 open / $268k.
   Ledger orphans: 105 → **0**.
3. **Sweep ran** — `sweep-once.ts base`: **0 closed / 122 open**, byReason {}.
   No exit policy tripped at current marks. Realized PnL still $0.
   - Had to fix `sweep-once.ts`: top-level await fails under tsx cjs (root
     package.json has no `type: module`; packages/api does). Wrapped in async
     main(). Original backed up to `scripts/sweep-once.ts.bak`.
4. **Watchdog** — TRADER block: fills 122 (100% quote-backed), round trips 0,
   realized PnL $0, exit reasons none, open positions 122, orphans 0, stale
   builds none. NEW alert: open exposure exceeds funded capital (backfilled
   $231k now visible; catalog strategies $12k–$42k vs $10k–$12k funded).

## Comms (WBTC) — HOLD
`comms-agent-cron-run.sh` + `sessions/.comms-cron.env` missing on iMac; cron
`59 23 * * *` failing since Sep 3 (last real run Aug 31). Deploy blocked on:
(a) Julius puts `COMM_AUTOMATION_SECRET` in abra project `gotchibot`,
(b) Julius decides whether the ~6-day newsfeed backlog should post.
Then: `abra run gotchibot -- COMMS_CRON_SCHEDULE="59 23 * * *" node
scripts/comms-agent-cron-deploy.mjs` (keep 23:59; script default is 16:00 UTC).