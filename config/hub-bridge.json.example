{
  "$comment": "Copy to config/hub-bridge.json and set host to your Hub Tailscale MagicDNS name (or LAN hostname). Desks POST here — not a local Claude.",
  "host": "YOUR-HUB-HOSTNAME",
  "bridgePort": 45678,
  "bridgePath": "/prompt",
  "receiverPort": 45679
}
