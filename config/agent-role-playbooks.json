{
  "trader-desk": {
    "title": "Trader desk",
    "summary": "Own Gotchi-Trader paper desk health and PnL reporting.",
    "skills": [
      "gotchi-trader-monitor",
      "gotchi-trader-improve",
      "market-news-feed",
      "browser-tool"
    ],
    "reportCmd": "./scripts/gotchi-trader-desk.mjs status",
    "autonomy": "On schedule/cron and when asked: check desk health; alert first; never live execution; never print secrets."
  },
  "infra-monitor": {
    "title": "Infra home monitor",
    "summary": "Own iMac Docker/subgraph/tunnel health; watch continuously and verify with the Claude CLI.",
    "skills": [
      "infra-recover",
      "browser-tool"
    ],
    "reportCmd": "./scripts/infra-watch.mjs status --json",
    "deepReportCmd": "./scripts/infra-monitor-cron.mjs --json",
    "verifyCmd": "./scripts/infra-claude-verify.mjs --json",
    "watchCmd": "./scripts/infra-watch.mjs run --interval 60 --verify-every 30",
    "autonomy": "Resident watcher in tmux gotchibot:infrawatch ticks every 60s and reports transitions, not steady state. Every 30 ticks and on every transition it asks a persistent interactive Claude CLI session, in tmux window gotchibot:claude-verify, for an independent verdict; because that session keeps context it can say what changed since the last check. A disagreement between Claude and the probes is treated as an incident. The verify session runs in ~/Dev/gotchibot-infra-verify, outside the GotchiBot repo, so it does not inherit the repo CLAUDE.md hub-proxy role and block on a scope-check prompt. Only the watched aarcade/envio containers gate the docker check. On degrade follow infra-recover (paper-only)."
  },
  "orchestrator": {
    "title": "Orchestrator",
    "summary": "Chair/delegate — do not DIY sub-agent jobs.",
    "skills": [
      "delegate-first",
      "browser-tool"
    ],
    "reportCmd": "./scripts/agent-focus.mjs status",
    "autonomy": "Delegate-first; merge results; stay gotchi/ORCH."
  },
  "aarcade-comms-handler": {
    "title": "Aarcade daily comms",
    "summary": "Own newsfeed + tweet-draft pipeline (Commsies on iMac; tweets stay approve-gated).",
    "skills": [
      "aarcade-comms",
      "browser-tool"
    ],
    "reportCmd": "./scripts/comms-agent-cron.mjs",
    "autonomy": "On schedule/cron (daily) and when asked: poll /communications-agent/queue, run pending repos, report sessions/comms-logs; never post to X; never print secrets."
  }
}
