# Standing duty: infra-hub (composable)

Optional subset of home infra. Owns Hub OpenClaw gateway
(`./scripts/gotchibot hub status`). Wire:

```bash
gotchibot templates apply <role> --hero <hero> --standing-duty infra-hub --yes
```
